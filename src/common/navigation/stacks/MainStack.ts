import { createNativeStackNavigator } from '@react-navigation/native-stack'

import { MainStackNavigatorParamList } from '../types'

export const MainStack = createNativeStackNavigator<MainStackNavigatorParamList>()
